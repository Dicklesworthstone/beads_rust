# Security audit configuration
# https://docs.rs/cargo-audit/latest/cargo_audit/config/index.html

[advisories]
# No active exceptions. The former RUSTSEC-2025-0134 (rustls-pemfile,
# unmaintained) ignore was dropped on 2026-09-17: rustls-pemfile no longer
# appears in Cargo.lock — asupersync 0.5.0 / fastmcp-rust 0.10.0 no longer
# depend on it (beads_rust-0edxa).
